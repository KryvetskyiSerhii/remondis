import { createSlice, PayloadAction } from "@reduxjs/toolkit";

const initialState = {};

const inspectorsSlice = createSlice({
  name: "inspectors",
  initialState,
  reducers: {},
});

export const {} = inspectorsSlice.actions;

export default inspectorsSlice.reducer;
